# macOS XQuartz; enable network clients and allow only the Docker host.
services:
  ros2sample:
    environment:
      DISPLAY: host.docker.internal:0
      QT_X11_NO_MITSHM: "1"
      LIBGL_ALWAYS_SOFTWARE: "1"
